class_name ExplorationText extends Control #podria ser dialogue text, tipo mas generico yq ue se use en todos lados sinceramente
#explorar el addon dialogic o algo así
@export var frame: TextureRect
@export var speaker: TextureRect
@export var s_name: Label #speaker's name
@export var message: Label

# Called when the node enters the scene tree for the first time.
func _ready() -> void:
	pass # Replace with function body.


# Called every frame. 'delta' is the elapsed time since the previous frame.
func _process(delta: float) -> void:
	pass
